# Web app dependencies. Localization dependencies (opencv-python, numpy)
# are already declared in the repo root requirements.txt -- install both:
#
#   pip install -r requirements.txt -r webapp/requirements.txt
#
Flask==3.1.3
gunicorn==23.0.0
